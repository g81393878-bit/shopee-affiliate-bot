# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
*.so

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment variables — NEVER commit secrets!
.env
*.env
!.env.example
# Render env vars (real secret values for tools/render_set_env.py) — NEVER commit
tools/render_env.local.json
tools/render2_env_export.txt
tools/client_secret*.json
client_secret*.json
tools/youtube_token*.json
youtube_token*.json
tools/*.gs
telegram-smart-memo-*.json
*.mp3

# Cron setup doc with real CRON_TOKEN — NEVER commit! (template: cron-setup.example.md)
docs/cron-setup.md

# Databases
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal

# Generated exports / imports (CSV/XLSX from tools/ — run locally, never commit)
*.csv
*.xlsx
# ยกเว้น: เทมเพลตชีทสำหรับ lean-stack (ของที่ต้องส่งให้ลูกค้าโหลด)
!lean-stack/template.xlsx
!docs/sample-products.csv

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node / Frontend build
node_modules/
dist/
.cache/

# Logs
*.log
logs/

# Test coverage (pytest-cov)
.coverage
htmlcov/

# Browser drivers (binary tools — do not commit)
chromedriver.exe
chromedriver*/
geckodriver*/
geckodriver.exe
video_preview/
*.zip

# Fonts auto-downloaded by tools/setup_rich_menu.py (fetched from Google Fonts on demand)
backend/tools/Prompt-*.ttf

# Jupyter notebooks with sensitive data
*.ipynb

# App-specific local data
.freebuff/

# Config backups (contains .env/cookies/db-password — never commit)
backups/

# Hermes AI generated market diary (runtime data — regenerated by tools/hermes_brain.py)
MARKET_MEMORY.md

# Supabase CLI linked project (contains pooler URL with DB password)
supabase/.temp/

# Shopee session data (contains session cookies + personal info)
affiliate_open_api_application.json
.tmp_cookies/

# Temporary images at repo root (screenshots, exported backgrounds) — do not commit
/*.png
/*.jpg
/*.jpeg
/*.webp

fb_cookies.json
.promo_caption_state.json
.promo_poster_state.json

# Reels uploader (uploader.py) — คลิป/state runtime ไม่ commit (เก็บ .gitkeep ไว้)
*.mp4
reels_uploader/pending_videos/*
!reels_uploader/pending_videos/.gitkeep
reels_uploader/posted/*
!reels_uploader/posted/.gitkeep
pending_videos/*
!pending_videos/.gitkeep
posted/*
!posted/.gitkeep
last_post_time.txt
posts_today.txt
uploader_execution.log
.uploader_intro_state.json
.reels_notify_state.json

