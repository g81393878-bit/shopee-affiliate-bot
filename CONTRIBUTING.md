# 🤝 ร่วมพัฒนาโปรเจกต์ — ป้าเข็ม ขายของ

ขอบคุณที่สนใจมาช่วยพัฒนา! โปรเจกต์นี้เป็น **บอท LINE ค้นหาสินค้า Shopee** ที่เขียนด้วย
Python (FastAPI) — เปิดให้ทุกคนช่วยทำให้ดีขึ้นได้ ไม่ว่าคุณจะเก่งโค้ดหรือไม่ก็ตาม

---

## 💡 ช่วยได้หลายแบบ (ไม่ต้องเขียนโค้ดก็ได้)

| แบบ | ทำอะไร | เหมาะกับ |
|---|---|---|
| 🐛 **รายงานบั๊ก** | เจอบอทตอบผิด/ล่ม → เปิด Issue บอก | ทุกคน |
| 💡 **เสนอไอเดีย** | อยากให้มีฟีเจอร์ใหม่ → เปิด Issue เสนอ | ทุกคน |
| 📖 **เขียน/แปลเอกสาร** | แก้ README, คู่มือ, แปลเป็นภาษาอื่น | ไม่ต้องเก่งโค้ด |
| 🛠️ **แก้โค้ด** | แก้บั๊ก / เพิ่มฟีเจอร์ → ส่ง Pull Request | คนเขียนโค้ดได้ |

---

## 🚀 วิธีส่งโค้ด (Pull Request) ทีละขั้น

> Pull Request (PR) = "ส่งโค้ดที่แก้แล้วกลับมาขอให้เจ้าของรวมเข้าโปรเจกต์หลัก"
> โค้ดจะเข้าโปรเจกต์ได้**ก็ต่อเมื่อเจ้าของกดรับ (merge)** เท่านั้น — ไม่มีใครแก้โค้ดหลักได้เอง

### ขั้นที่ 1 — Fork (ก๊อปไปไว้บัญชีตัวเอง)

1. เปิดหน้า repo: `https://github.com/g81393878-bit/shopee-affiliate-bot`
2. กดปุ่ม **Fork** (มุมขวาบน) → จะได้สำเนาโปรเจกต์ในบัญชี GitHub ของคุณเอง

### ขั้นที่ 2 — Clone (ดาวน์โหลดลงเครื่อง)

```bash
git clone https://github.com/<ชื่อบัญชีคุณ>/shopee-affiliate-bot.git
cd shopee-affiliate-bot
```

### ขั้นที่ 3 — สร้าง Branch ใหม่ (แยกสายงานของตัวเอง)

> ห้ามแก้บน branch `main` โดยตรง — สร้าง branch ใหม่สำหรับงานแต่ละชิ้น

```bash
git checkout -b fix/ชื่อสิ่งที่แก้
```

ตัวอย่าง: `fix/ค้นหาฮิตไม่เจอ`, `feat/เพิ่มปุ่มเทียบสินค้า`

### ขั้นที่ 4 — แก้โค้ด

- แก้ไฟล์ตามเรื่องที่อยากทำ (1 PR = 1 เรื่อง อย่ายำหลายเรื่อง)
- ทดสอบก่อนส่งเสมอ:
  ```bash
  python -m py_compile backend/app/*.py backend/app/api/*.py
  ```

### ขั้นที่ 5 — Commit + Push

```bash
git add <ไฟล์ที่แก้>
git commit -m "อธิบายสิ่งที่ทำสั้นๆ"
git push origin fix/ชื่อสิ่งที่แก้
```

### ขั้นที่ 6 — เปิด Pull Request

1. กลับมาที่ repo ของคุณบน GitHub → จะเห็นปุ่ม **Compare & pull request**
2. กด → เขียนอธิบายว่าแก้/เพิ่มอะไร (และทำไม)
3. กด **Create pull request** → เจ้าของจะมารีวิวแล้ว merge ให้

---

## 📋 กติกา (สำคัญ — อ่านก่อนส่ง)

1. 🚫 **ห้าม commit ไฟล์ลับ** — `.env`, key, token, password, ไฟล์ `.db`/`.csv` (ดู `.gitignore`)
2. 📦 **1 PR = 1 เรื่อง** — อย่ารวมงานหลายอย่างใน PR เดียว (review ง่ายกว่า)
3. ✍️ **อธิบายให้ชัด** — ใน PR เขียนว่า "แก้/เพิ่มอะไร + เพราะอะไร + ทดสอบยังไง"
4. 🧪 **ทดสอบก่อนส่ง** — โค้ดต้องรันได้ ไม่พังของเดิม
5. 🎨 **ทำตามสไตล์เดิม** — ข้อความที่ลูกค้าเห็นใช้ภาษาไทย, โค้ด/comment ตามที่เห็นในไฟล์เดิม

---

## 🐛 วิธีเปิด Issue (รายงานบั๊ก / ขอฟีเจอร์)

1. ไปที่ tab **Issues** → กด **New issue**
2. ตั้งชื่อ + อธิบาย:
   - **บั๊ก:** อะไร / ทำยังไงถึงเจอ / ควรเป็นแบบไหน / screenshot (ถ้ามี)
   - **ฟีเจอร์:** อยากได้อะไร / ใช้แก้ปัญหาอะไร

---

## ⚖️ License

โค้ดทั้งหมดอยู่ภายใต้ **Custom License** (ดูไฟล์ `LICENSE`): ใช้ส่วนตัว/เรียนฟรี แต่
**ห้ามใช้เชิงพาณิชย์/ขายต่อ** โดยไม่ได้รับอนุญาตจากเจ้าของก่อน

การส่ง Pull Request = คุณตกลงให้ผลงานของคุณอยู่ภายใต้ license เดียวกันนี้

---

## 💬 ติดต่อ

มีคำถามก่อนเริ่ม? เปิด Issue ถามได้เลย หรือติดต่อเจ้าของโปรเจกต์
